name: Build and Deploy to Itch.io

on: push

jobs:
  buildForWindows:
    name: Build for StandaloneWindows
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          lfs: true

      - uses: actions/cache@v3
        with:
          path: Library
          key: Library-StandaloneWindows
          restore-keys: Library-

      - uses: game-ci/unity-builder@v4
        env:
          UNITY_LICENSE: ${{ secrets.UNITY_LICENSE }}
          UNITY_EMAIL: ${{ secrets.UNITY_EMAIL }}
          UNITY_PASSWORD: ${{ secrets.UNITY_PASSWORD }}
        with:
          targetPlatform: StandaloneWindows
          buildPath: build/StandaloneWindows

      - name: List Build Directory Contents
        run: ls -R build

      - uses: actions/upload-artifact@v3
        with:
          name: Build-StandaloneWindows
          path: build/StandaloneWindows

itchio-deploy:
    name: Deploy to Itch.io
    needs: buildForWindows
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          lfs: true

      - name: Download Build Artifact
        uses: actions/download-artifact@v3
        with:
          name: Build-StandaloneWindows
          path: build/StandaloneWindows

      - name: Install Butler
        run: |
          curl -L -o butler.zip https://broth.itch.ovh/butler/linux-amd64/LATEST/archive/default
          unzip butler.zip -d /usr/local/bin
          chmod +x /usr/local/bin/butler
          /usr/local/bin/butler -V

      - name: Deploy to Itch.io
        env:
          BUTLER_API_KEY: ${{ secrets.BUTLER_API_KEY }}
        run: |
          /usr/local/bin/butler push build/StandaloneWindows theoscreation/itchdeploygithubtest1:platform-windows